# OpenCode installation

Add this git-backed plugin to `opencode.json`:

```json
{
  "plugin": ["product-deep-research@git+https://github.com/niko0xdev/skills.git"]
}
```

Restart OpenCode. Use the native `skill` tool to load `product-deep-research`.
